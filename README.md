# writing
Just a spot to keep my writings.

## Scripts

- `new_draft.py` / `new_draft.sh`: Automatically creates daily numbered Markdown drafts in `MarkParis/drafting/` with YAML frontmatter and opens them in Vim (runs via `uv`).
